services:
  - type: web
    name: quizai
    runtime: node
    buildCommand: npm run render-build
    startCommand: node server/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
